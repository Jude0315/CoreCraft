const Header = () => {
  return (
    <header className="page-heading">
      <span>CoreCraft console</span>
      <h1>Build full-stack apps from requirements.</h1>
      <p>
        The synthesis core translates generation progress into motion, glow,
        rotation, pulse, and particle intensity while pages stay focused on
        application state.
      </p>
    </header>
  )
}

export default Header
